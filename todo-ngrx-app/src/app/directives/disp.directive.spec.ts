import { DispDirective } from './disp.directive';

describe('DispDirective', () => {
  it('should create an instance', () => {
    const directive = new DispDirective();
    expect(directive).toBeTruthy();
  });
});
